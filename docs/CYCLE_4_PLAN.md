# Cycle 4 — location-aware expansion alternatives

Hypothesis recorded before implementation and development games: treating empty crop tiles as interchangeable can exclude profitable nearby sites in a new quadrant. Permit expansion batches that actually use a new tile even when more than six owned crop sites are vacant. Keep the current forecast, profitability ranking, cash reserves, dispatch, fertilizer timing, opening and hiring rules unchanged.

The three supplied public episodes exactly match Cycle 3's source in all 2,157 decisions. They contain one win and two losses, with clean own execution. In episode 107763520, state 313 (Day 14, hour 1), the farm holds 16,230 coins and 32 crops; all eleven enumerated investment alternatives fail the forecast's route check, and eight vacant owned crop sites prevent enumeration of a third quadrant. This is a candidate-generation restriction, not proof that expansion will win. Preserve `baselines/cycle_3.py` at SHA-256 `47c281bfb4118a9755f1759930b162de007efdfef3b23144457eccbaac42da0c`.

1. Reconcile the three replays and own runtime logs; archive compact provenance, cash, physical output, timing and execution evidence. Recorded-action replay is an audit, never a reactive counterfactual opponent.
2. Implement one isolated spatial-admission challenger. Reject duplicate paid-land options that do not use a locked tile. Charge the existing land cost and keep every downstream feasibility and working-capital constraint. Verify the intended decision on recorded states without describing it as a won counterfactual game.
3. Run a bounded development comparison on seeds 17/43, both seats, against frozen Cycle 3, Step 8 and the scaled mixed control. Compare matched Cycle 3 reference games. Measure match score, cash, productive footprint, wages, input/land costs and execution failures. Investigate any lost crop, missed feed, overflow or terminal stock before promotion. If this single change is weak, retain Cycle 3 and record rejection rather than tune many variants.
4. Only if promising, freeze the exact artifact and protocol before a new evaluation on seeds 9301–9320, both seats and the same three opponents (120 games per policy). Require a positive paired whole-seed 95% bootstrap score interval, no aggregate opponent-class score regression and no unresolved operational regression. The internal pool is limited and does not predict the leaderboard.
5. If qualified, run the full tests, lint, format and isolated official-loader full-season validation on the exact release file, then promote locally and push. Otherwise keep `main.py` unchanged. No Kaggle upload during implementation; the user receives an exact artifact and status. Record the new supplied games as Cycle 3 public evidence without inventing its submission ID or a validation episode.

Local CPU only, $0 new spending. The broader staffing redesign remains parked. Server observations and all used seeds are development evidence once inspected; do not reuse them as an untouched evaluation set.
