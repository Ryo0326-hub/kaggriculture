# Cycle 2 — staffing and ordinary-night routes

Hypothesis frozen before development experiments: Step 8's dispatch hiring estimate charges repeated individual crop trips even though its crop dispatcher already permits ordinary-night automatic deposit. A dated crop-route allowance, plus its separately committed livestock crew, may reduce wages without reducing banked output. Ordinary-day routes can omit terminal return travel; final-day routes must deliver goods explicitly.

Compare installed 25/45/65-asset portfolios on one/two/three owned quadrants, both seats, development seeds 17 and 43. All start with the same preinstalled assets, cash and inputs; no new animals, land or seeds may be purchased. Start crops watered to represent prepared installed assets. Test unchanged Step 8, a dispatch-only challenger, and forecast-only/combined variants separately in standard-start games only after operations pass. Modified-start cash is not a competition score.

Measurements: executed wages, actual harvested/collected output, cash, effective actions, starvation/escapes, crop decay, explicit/overnight overflow, terminal stock, and forecast staffing error. Preserve per-date staffing and job counts. Route bounds are heuristic feasibility estimates, not an optimality certificate for the dispatcher.

Reject staffing reductions that introduce accidental economic losses relative to a clean reference. Existing reference failures on deliberately large fixtures must be reported; do not call those fixtures clean validation. Keep the final delivery rule and shared inventory reservations. If the hypothesis fails, preserve the rejected experiment and retain Step 8.

Use only local CPU compute. Inspected seeds 9101–9120 are development data. Freeze any promising challenger and its controls before a separate fresh-seed benchmark. Main's callable entry point remains Step 8 behavior during development; the exact Step 8 artifact remains in baselines/step_8.py. A new release needs tests, isolated packaging, and explicit accounting for the latest-two submission window.
